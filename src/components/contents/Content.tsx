import Header from "../header/Header";
import{Main} from "./Content.styled";
import React from 'react';

export default function Content(props:any){
    // const planets= props.data.find((planet:any) => planet.name);
    return(
        <Main>
            <Header></Header>

        </Main>

    );
}